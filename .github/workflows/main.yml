on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: waws-prod-blu-515.ftp.azurewebsites.windows.net
        username: ${{ secrets.brajesh\$brajesh }}
        password: ${{ secrets.berxEoxjaXod0RdntQnp4pydXGn2Xoxj5ucTfZ0eX6n8olFLgDvYWotE1FJY }}
        protocol: ftps        
        server-dir: /site/wwwroot/wp-content/themes/astra-child/
        local-dir: ./wp-content/themes/astra-child/
        # dry-run: true
